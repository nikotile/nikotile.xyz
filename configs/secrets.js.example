const user = 'user';
const password = 'password';
const cluster = 'cluster';
const dbName = 'database';

module.exports = {
  dbUri: `mongodb+srv://${user}:${password}@${cluster}.wfjp2.mongodb.net/${dbName}?retryWrites=true&w=majority`
};
